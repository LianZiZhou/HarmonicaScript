- M0 — Toolchain, skeleton, and the two build-path spikes (S, 2d). Install .NET 10 SDK (action item zero: both installed SDKs die 2026-11-10); global.json pinned 10.0.100/latestFeature; .slnx; Directory.Build.props + Directory.Packages.props; .gitattributes (*.verified.* -text, testdata/**/*.mid binary); git init; MIT LICENSE; NOTICE.md; docs/PROVENANCE.md started. All 16 projects created with correct references and IsAotCompatible flags. ci.yml on ubuntu-24.04 + macos-26; windows.yml on windows-2025. DONE = `dotnet build && dotnet test` green locally and on all three runners with one trivial assertion in each suite; AND the two spikes that could invalidate two later milestones both pass: (a) an Avalonia 11.3.21 window opens on net10.0 on macOS arm64 (XAML compiler task + native asset resolution, not just lib resolution); (b) `dotnet publish -r win-x64 --self-contained -p:PublishSingleFile=true` from the Mac produces an .exe that LAUNCHES on the windows-2025 runner (dotnet/sdk#36023 could not be confirmed closed) and whose output contains no DryWetMidi native blobs. Two 30-minute checks guarding two-week bets.
- M1 — Instrument profiles, emission table, candidate table (S, 3d). Instrument/timing/keytable/game/diagnostics-rules schemas with STJ source-gen; embedded defaults; the four-level override chain (embedded → AppDir/data → %LOCALAPPDATA% → .hsproj snapshot) with per-field origin; hand-written IProfileMigration classes; the GENERIC profile validator (derived invariants only). ModState space derivation. EmissionTable. DONE = `hsc profile candidates` prints every (degree, state) rendering for all 38 offsets and matches BOTH a reviewed golden file AND an exhaustive assertion against the closed formula; `hsc profile dump --effective` shows the origin of every field; the pinned df.harmonica.v1 fixture test asserts 6 states, 38 offsets with zero uncovered, 48 renderings, popcount {1:30,2:6,3:2}, 2-way exactly {-7,1,5,13,17,24}, 3-way exactly {0,12}, and that a state uniquely determines the key; the cross-constant timing relation check rejects a deliberately inverted profile.
- M2 — MIDI ingest, corpus, and the fixture generator (M, 4d). Tolerant ReadingSettings + GB18030 + Sanitize; SourceSong; track inventory + MelodyScore; LoaderVersion/SanitizeSettingsHash stamping. Commit 25-30 public-domain .mid files (Bach, 欢乐颂, 小星星, IMSLP/Mutopia classical, traditional 古风). Build the procedural fixture generator (code + fixed seeds, DryWetMidi writes them): chromatic runs, band-seam crossings at offsets 0/12, 4-second sustains, dense sixteenths at the retrigger floor, forced L↔R exclusive swaps, >38-semitone range, rolled chords at 40-50 ms, multi-tempo, malformed. Manifest + fetch script for a private extended corpus. DONE = every committed fixture and every generated fixture parses with zero exceptions; a deliberately corrupt fixture (KeySignature scale=255, truncated chunk, orphaned NoteOn, GBK track name) loads instead of throwing; Verify snapshots of MIDI→SourceSong committed; testdata/README.md documents the copyright rule so nobody helpfully adds 起风了.mid.
- M3 — Reduction pipeline as a bounded fixpoint, with counters (M, 3d). IScorePass, PipelineContext (mutation contract stated), PassCounter, Diagnostic-carries-NoteId. reduce.chords (45 ms musical-time window, earliest onset, siblings retained) / reduce.monophony / reduce.dropShort (derived floor) / reduce.smoothMelody (promote-sibling-then-drop), repeating until unchanged, max 4. time.speed after the group. DONE = the strict-monophony invariant holds on every committed and generated fixture under all four reduction policies; the fixpoint converges within 4 on all of them (ReductionNotConverged is zero); a CsCheck property asserts emitted pitch content ⊆ source and that dropped/sibling notes are retained in the list; per-pass counters snapshotted.
- M4 — The modifier-planning DP (M, 5d). ModifierPlanner with the exact integer nodeCost/edgeCost above, k derived from the profile, precomputed 6x6 tables, flat rolling long[k] and byte[k*N], zero allocation in the hot loop, pessimistic clamped availSil, marginal deficit. DONE = a brute-force equivalence property test — 2,000 random instances with N ≤ 6 in CI (plus N ≤ 8 x 100 nightly) with EXACT int64 equality of the optimum, using a generator biased hard toward the multi-way offsets {0,12} and {-7,1,5,13,17,24} and toward adjacent pairs forcing exclusive swaps (a uniform generator yields mostly trivial instances, since 30 of 38 offsets have exactly one feasible state); plus a monotonicity test (raising W_EVENT never raises the optimum's event count); plus a self-consistency test that the DP's reported cost equals a cost recomputed from the emitted plan; plus a soundness test asserting every edge the DP marks feasible is realisable by truncating the previous note alone. Solver wall time < 50 ms for N = 2,000 across all candidates.
- M5 — Transposition search with the tiered objective (M, 3d). Exact bounds, no prune, LegaliseRange inside the loop, re-run monophony, the four TranspositionModes, tiered comparison with one epsilon, deterministic final tie-break, top-5 candidates retained with all four tier values. DONE = the degeneracy regression suite passes — 12 synthetic pieces, one per key, each fully diatonic, all resolve to |signedMod12(t)| minimal; an all-sharp piece is NOT transposed by −1; a piece whose range forces t=+3 is not moved to t=+4 by any mechanical saving (the exact case the winning plan's weighted sum lost); and a long piece with three infeasible exclusive swaps loses to a candidate that folds 0.3% of duration (the exact case the normalised-by-N objective lost). Plus: running the search twice yields byte-identical results, and no float appears anywhere in the optimiser (asserted by a Cecil test).
- M6 — Articulation, lowering, hold caps, validator (M, 4d). The resolution ladder with drift guard; sched.breath (default on); emit.lower for BOTH hold and toggle; emit.holdCaps splitting notes with modifier runs; emit.quantise (the one rounding point); TimelineValidator as a logical state machine including the lead-time invariant. DONE = the validator reports zero violations on every committed and generated fixture under BOTH timing profiles and BOTH lowering modes; a toggle-lowered timeline that would leave a modifier latched is REJECTED (the invariant the winning plan's validator could not see); an adversarial fuzz (1 ms inter-onsets, alternating extreme octaves, zero-length notes, 300 nps bursts) still produces a valid timeline with nothing held or latched at the end; DeficitEventCount is nonzero only where the ladder used merge or drop; golden timelines snapshotted for both lowerings.
- M7 — InstrumentSimulator round-trip + audition (M, 3d). The simulator computed from DegreeSemitones/ModifierSpec directly, modelling pitch as a step function over time. MeltySynth 2.4.1 + a small GM SoundFont → 44.1 kHz WAV, offline only, no audio backend dependency. DONE — the project's central correctness gate: for every fixture, (a) Simulate(timeline) yields exactly the audible score's EffectiveMidiNote sequence with onsets within maxOnsetShiftMs, (b) sounded pitch is CONSTANT across each note's [down,up) span, and (c) the non-circular assertion EffectiveMidiNote == SourceMidiNote + t holds for every note not flagged OctaveFolded/Dropped/ChordSibling. Run across both timing presets and both lowering modes. A deliberately broken planner (modifier armed one note early) MUST make it fail — that negative test ships alongside. `hsc audition song.mid -o /tmp/song.wav` produces an audible file on macOS. SimulatedNote[] is golden-filed; the WAV never is (float/SIMD differences across CI architectures make audio byte-comparison flaky).
- M8 — CLI + generic exporters incl. AHK v2 (M, 4d). `hsc convert|score|timeline|audition|export|play|selftest|diag|replay|profile|contrib|goldens|watchdog` on System.CommandLine 2.0.12. hscore-json/csv, reduced-mid, keytape, jianpu-hsq, ahk-v2, plus the shared lowering chain and readers for the four self-describing formats. The canonical TraceWriter/TraceReader. DONE = every core capability is reachable headlessly with no GUI assembly loaded; Parse(Write(t)) == t for the four readable formats over every fixture; the reduced .mid reopens in DryWetMidi with an identical monophonic pitch/onset sequence; CumulativeDriftMs < 1 ms over 5 minutes; and — the criterion that matters — the windows-2025 job EXECUTES the generated .ahk with Send stubbed to a log and the log diffs clean against the InputTimeline.
- M9 — v0.1.0-alpha public release, and the feedback loop as a product feature (M, 3d). MIT LICENSE; docs/PROVENANCE.md complete; docs/CALIBRATION.md with the six scripted in-game experiments (each a golden-filed timeline plus one yes/no question); the blocking bilingual disclaimer; the Chinese-first README with the 「未经实机验证的部分」 list; `hsc diag` bundle builder with redaction; `hsc replay`; four GitHub issue templates; release.yml producing an uncompressed portable zip (primary) plus a single-file variant; SHA-256 + VirusTotal links; 蓝奏云 mirror; a Bilibili post that explicitly ASKS for three testers with the game. DONE = v0.1.0-alpha is downloadable and a stranger can convert a MIDI and produce a runnable .ahk without any installer and without the process making a single network call. This release is deliberately CLI-first and deliberately labelled a tester recruitment build; the public-facing beta is M13.
- M10 — Windows CI hardening (S, 2d). On windows-2025: run the SendInput backend against a real Win32 target window and assert the exact characters arrive (validating the Set-1 table, the extended flag, the 0x000A key-up path and single-call batching end-to-end on genuine Windows); run the absolute-deadline scheduler and record the p99 deadline error; run the RegisterHotKey pump thread; run the P/Invoke marshalling and RecordingSendInputApi byte assertions. DONE = all four green on every push, and roughly 70% of what the winning plan treated as hardware-blocked is now a check on every commit. None of this involves Delta Force.
- M11 — Weight tuning, provenance, and the golden review gate (M, 3d). Objective weights move to data/weights/objective.v1.json, each with a Provenance badge; the MECHANICAL weights are marked Invented and pinned to the TimingProfile they were guessed against, because a MeltySynth render cannot tell you what the game does with an insufficient gap. `hsc goldens review` emits an aggregate delta table (notes dropped ±, folded ±, modifier transitions ±, longest churn run ±, chosen transpose, playability ±, peak nps ±) across all fixtures. A fixed 10-file ear set. DONE = weights frozen with a written rationale per weight; a CI check fails any change to *.verified.* that lacks a GOLDEN-CHANGE: trailer and the delta table; the ear set has been listened to once, deliberately.
- M12 — Avalonia shell v1: import, report, export (L, 8d — the largest single block and the one most likely to overrun for a developer new to Avalonia). Screens S1; full zh-Hans/en-US with the observable Loc indexer and runtime switching; the stacked penalty bar with click-to-filter; the transposition candidate radio list; timing values rendered with Provenance badges; the first-run blocking disclaimer; the amber unverified badge. DONE = a complete MIDI → report → export round trip performed entirely in the GUI on macOS; the resx key-parity and DiagnosticCode-parity tests pass; toggling 中/EN changes every visible string with no restart.
- M13 — Note editor, journal, .hsproj, rehearsal view, and the v0.5.0 public beta (L, 8d). Screens S2/S3/S4: inline degree/accidental/octave/mute editing, PromoteChordSibling, the SourceIndex-anchored ScoreEdit journal that IS the undo stack, .hsproj save/open with the profile-snapshot field diff and content-fingerprint re-anchoring, the read-only piano-roll strip, and the rehearsal view driven by the SAME InstrumentSimulator. DONE = a project saved by an older build opens under a newer converter, re-runs the pipeline, KEEPS its edits and reports how many were re-anchored versus orphaned; the rehearsal view is frame-accurate against the golden timeline in a headless test; v0.5.0 ships as the public-facing GUI beta. Note this is the fallback product in full if Branch A proves non-viable.
- M14 — Branch A: Windows backend, preflight, self-test, diagnostics (M, 6d). CsWin32 behind ISendInputApi; WindowsSendInputBackend; MacCgEventBackend + the loopback.text.v1 profile and TextEdit readback; the absolute-deadline scheduler with high-resolution waitable timer, spin tail, MMCSS and timeBeginPeriod(1); RegisterHotKey F6/F7 on a dedicated pump thread; the per-batch foreground guard; the layered release-all including the session-start batch and the out-of-process watchdog; IEnvironmentProbe + the pure PreflightRules; the Notepad delivery test with CORRECTED routing; screens S5/S6. DONE = every Playback invariant green against TraceBackend + VirtualClock on macOS, including a fault-injection theory (every fixture x every 17th event x six abort modes) asserting the trace always ends in RELEASEALL with an empty held set; PreflightRules golden-filed across synthetic fact sets; the macOS loopback plays a whole fixture into TextEdit and the read-back text matches exactly; the windows-2025 SendInput job stays green. Deliberately last so the unverifiable part never blocks the verifiable 90%.
- M15 — Vendor exporters, strictly sample-gated (M, 5d). Order: bloody-amc (+ identical .bmc), razer3-xml, logitech-lua, then redragon-msmacro. Each ships with a golden byte file, a decode fixture that parses the REAL vendor sample to a hand-written expected timeline, typed Quirks in its capabilities JSON, and ExtraInstructions surfaced in the UI. GATE, per exporter: one contributed sample containing right/middle mouse events (and DelayType=1 for msmacro). Until the sample arrives the exporter is registered but REFUSES with a named missing capability. DONE per exporter = the real sample parses; Write(Parse(sample)) differs only in normalised whitespace; the UI states plainly that importability has not been verified on hardware. Corsair and every unverified vendor stay out.
- M16 — v1.0 (S, 3d). Documentation pass; the profile marked verifiedInGame only once enough consistent user reports agree; the eight-item unverified list shrunk to what nobody can close; a decision on the Avalonia 12 migration; only then consider a second instrument profile. DONE = a tagged v1.0.0 with an accurate risk statement and a README whose unverified section is honest. REALISTIC TOTAL: ~62 developer-days of listed work; with the 1.5-2x correction the judges applied to all three source plans and no slack anywhere, budget 90-110 developer-days to v1.0, with v0.1.0-alpha at ~day 30 and the v0.5.0 GUI beta at ~day 55.